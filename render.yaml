services:
  - type: web
    name: calgarycommunity-django
    env: python
    plan: free
    runtime: python
    buildCommand: |
      apt-get update &&
      apt-get install -y binutils libproj-dev gdal-bin &&
      pip install --upgrade pip &&
      pip install -r requirements.txt
    startCommand: "gunicorn calCrimes.wsgi"
    envVars:
    - key: DATABASE_URL
      value: postgresql://postgres:Urking1!@db.flsgrbtpuhbgdpsokfss.supabase.co:5432/postgres